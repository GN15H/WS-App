
export type RoomMap = {
  id: number,
  name: string,
  description: string,
  active_users: number
}

export type Room = {
  id: number,
  name: string,
  description: string,
  activeUsers: number
}
